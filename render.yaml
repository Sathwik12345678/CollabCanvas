services:
  - type: web
    name: collabcanvas-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install --prefix server
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
